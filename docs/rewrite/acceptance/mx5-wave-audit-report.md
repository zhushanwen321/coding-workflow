# mx-5 波次终验：设计-实现一致性对抗审查报告

> 审查日期：2026-08-20。审查对象：mx-5 波次五 unit 全部交付（mx5-1 `e29238e` / mx5-3 `9fd2c87` / mx5-2 `9703d9f` / mx5-4 `e1aa49b` / doc-4 `e2382ee`，收口 `de3c37d`）vs 设计文档 `docs/rewrite/design-spec-contract-replan.md`（`97804d5` 定稿态）。
> 审查方式：两个独立 reviewer 并行——A 负责核心机制域（D1 规则⑨ + D2 回炉通道，报告 `.tmp/mx5-audit-d1d2.md`），B 负责配套域与验收链（D3-D6 + V1-V6 + 拆分全序，报告 `.tmp/mx5-audit-d3d6.md`）。对抗式：默认怀疑偏离，逐条设计断言找实现反例。

## 结论

**mx-5 设计-实现一致性终验通过**：70 条设计断言逐条核对，**MUST_FIX = 0，实现缺失 = 0**；关键不变量反例攻击 8 条全部放行；验收链 V1-V5 全部真跑且达标、V6（gate 四跑）未跑与设计定位（波后观察项）一致；拆分全序（mx5-1∥mx5-3 → mx5-2 → mx5-4 → doc-4）时序实测遵守，mx5-2/mx5-4 串行理由经 diff 对抗核实成立。

偏离合计 5 SUGGESTION + 9 INFO（超设计 2 / 设计过时 2 / 完备性缺口 3 / 口径差 1 / 存量挂账与边界记档 6），无一项推翻设计承诺的语义。

## SUGGESTION 清单（修复建议待裁决）

| # | 域 | 描述 | 修复方向 |
|---|-----|------|---------|
| S1 (A·D-1) | D2 注释/词条 | 「封闭枚举」描述不完整：`parseError===true` 实际来源还包括 JSON 合法但形状不符、playwright/pytest 零条目且 exit 0 防线、translate 抛错（`runner:"e2e-sh"` 显式声明的 unit 型条目 command 缺省可绕过规则③到达）、路由旁路——回炉方向对这些形态同样正确，但 types.ts 注释与 CONTEXT.md 词条的枚举不完整，审计会漏判 | 注释/词条改准确（非穷举式表述或补全） |
| S2 (A·D-2) | D1 规则⑨ | `--reporter json`（空格形态）确定性漏网：规则⑨按设计「兼容两种形式」放行 json 值，但 u5 translate 幂等检查只认 `--reporter=json` 等号子串——空格形态会被 cw 再追加等号 flag，双 reporter 恒挂。设计文本自身缺口传导为实现缺口（D2 回炉兜底在，但违背 G1「拦得住的拦死」完备性） | 规则⑨收紧：只放行等号形态（存量 10 夹具文件全为等号形态，零翻红）；设计文档 D1 同步勘误 |
| S3 (A·D-3) | D1 规则⑨ | pytest 长选项前缀缩写（`--q`/`--quie`）不拦——argparse 前缀缩写同样触发 verbosity 相抵恒挂，低频 | 禁令匹配覆盖 `--quiet` 的合法前缀缩写 |
| S4 (B) | D3 清单口径 | reviewer 清单维度①按 type 分叉 vs 规则⑨按最终适配器路由分叉——`runner:"pytest"` 显式声明的 unit 型命令，问句不对口（机器层兜底在） | brief 问句补一句「runner 显式声明时按声明适配器核对」 |
| S5 (B) | D3 方案表 | 设计方案表「维度可被账本审计（verdict comment 结构化）」表述超前——实现是纯文本约定无结构化载体 | 设计文档措辞勘误（一行） |

## INFO 摘要（记档不修）

无关重提 spec 也烧回炉代数（设计定义如此）；gates 层 import verify/run 的 `adapterTypeFor`（防漂移正解、无循环依赖）；brief 模板「验收五规则」措辞未随九规则勘误；D6 停派态不含 TIMEOUT 封顶内存态（封顶后无新 spawn，可辩护）；brief.ts「缺分级视为无效审查」超设计一句（良性增强）；CONTEXT 回炉伴随词条超六词条清单（良性增强）；doc-4 基线先行 10 分钟早于 mx5-4 verified（依赖实现已落地，合规）；mx5-4 commit message 37 vs 基线 38 文件计数口径差（基线含自身）；mx5-3 报告行号漂移（后续波次所致）。

## V1-V6 验收链判定

| # | 载体 | 判定 |
|---|------|------|
| V1 | mx5-1 报告 §7（真实 CLI 毒 spec exit 1 + 三缺口全列 + A3 正确不列） | 真跑且达标 |
| V2 | mx5-2 报告（human spawn 全链 6 断言） | 真跑且达标 |
| V2b | mx5-2 报告（broken 恰 2 次 → deadlock、escalation 出声、10s 零派发） | 真跑且达标 |
| V3 | mx5-3 报告（3 次独立 pi spawn 3/3 fail、2 强命中；产物 /tmp/cw-mx53-v3/ 在档） | 真跑且达标 |
| V4 | mx5-4 + doc-4 报告拼合（零残留 + human spawn 链 + 六词条 + 534 绿） | 真跑且达标 |
| V5 | 各报告全量递增绿（510→527→534）+ 幂等语义用例 | 真跑且达标 |
| V6 | 未跑（M4 gate 四跑） | 设计定位波后观察项，ledger 状态一致 |

## 后续

- S1-S3 构成规则⑨完备性收口小 unit（spec-rules 收紧 + 注释/词条准确化 + 测试，预计单个 developer 一次交付）；S4/S5 各为措辞级小改——可合并同 unit。
- 修复后 M4 gate 四跑（V6）调度。
