/**
 * spec 提示词 — create action 后返回，指导 agent 如何明确编码任务范围与目标。
 *
 * 本模块是 CW engine 的一部分（非外部 skill 文档）。重构后 CW 是 agent-agnostic CLI，
 * guidance 直接携带方法论，不假设调用方有 skill 加载机制。
 *
 * 触发点：state-machine.ts buildNextAction 的 create 分支。
 * 交付物：plan.json（由 plan action 消费），落在 {workspacePath}/.xyz-harness/{slug}/。
 */

export const SPEC_PROMPT = `
[create 阶段] 明确编码任务范围与目标

你已经建立了 topic（cw create 已完成）。在写 plan 之前，必须先把"做什么"弄清楚。
本阶段不产出交付物给 CW，但决定 plan 的质量——跳过这步直接写 plan = plan 写偏。

## 必须弄清的三件事

1. 业务目标（做什么、为什么）
   - 一句话能说清"这个任务完成后，什么改变了"。说不清 = 还没想清楚。
   - 含可衡量的成功标准（不是"优化性能"，而是"P99 延迟降到 200ms 以下"）。
   - 含约束/不做边界（明确列出不做的事，防止范围蔓延）。

2. 技术现状（改什么、现在长什么样）
   - 读相关代码，理解当前实现。不读代码直接写 plan = 凭空臆断。
   - 列出要创建/修改的文件（文件级 + 每个文件的职责）。
   - 识别复用机会：现有 codebase 是否已有类似功能/可复用代码。

3. 歧义点（哪里不确定、需要谁拍板）
   - [先探索后提问] 澄清问题前先读相关代码、查项目文档。不读代码就提问 = 把探索成本转嫁给用户。
   - 把识别到的歧义分两类处理：
     - Discoverable（代码/文档能答的）：自己 grep/read 找答案，不问用户。
       如"这个函数在哪""项目用什么测试框架""有没有类似实现"。
       只有"多个候选都合理"或"找不到但需要"才升级问用户，且提问时附具体候选路径 + 推荐项。
     - Preferences/tradeoffs（代码答不了的）：问用户，但给 2-4 个选项 + 推荐默认值，不开放问。
       如"用方案 A 还是 B""阈值设多少""这个边界算不算"。
   - 区分阻塞性歧义（影响做什么）和细节性歧义（影响怎么做）。阻塞性必须先问。
   - [批量提问] 澄清问题批量提出，不逐个问。完成一轮探索后，把所有需用户拍板的
     Preferences/tradeoffs 问题收集起来，一次提交（1-4 个）。每轮只问一个 = 多轮往返成本。

## 范围守门（写 plan 前自检）

本任务是否真属于 lite 范围（单模块小功能，无架构改动）？以下任一出现 = 考虑拆分或升级流程：
- 跨 2 个及以上子系统/模块协调
- 状态机变更、核心数据模型变更、公共 API 契约变更
- 需要架构决策（技术选型、模块边界重划分）
- 改动影响 3 个以上既有文件的核心逻辑

范围判错了后面全白做。低判（本该拆分却一锅端）= 后期返工；高判（本该简单却过度设计）= 浪费。

## 本阶段禁止

- [禁止] 动手写实现代码（那是 execute 阶段的事）
- [禁止] 做架构设计/技术选型（只记录约束，不展开方案）
- [禁止] 跳过读代码直接臆断改动点

## 完成标志

三件事都弄清后，进入 plan 阶段：调 cw(action=plan, topicId, planJson) 提交结构化计划。
plan.json 的格式规范见 plan 阶段提示词。
`.trim();
